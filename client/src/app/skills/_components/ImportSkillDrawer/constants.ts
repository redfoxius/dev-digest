/** `accept` attribute for the file-upload input — archives converge on the
   same preview-then-confirm flow as a plain `.md`/`.markdown` file. */
export const FILE_INPUT_ACCEPT = ".md,.markdown,.zip,.tar,.tar.gz";
