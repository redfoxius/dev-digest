/** Keyword shortcuts shown as chips alongside the language filter — clicking
   one just seeds the free-text search (the `CommunitySkill` contract has no
   `tags` field to filter on structurally). Matches the design mock's
   "All languages / TypeScript / security / performance" chip row. */
export const TAG_CHIPS = ["security", "performance"] as const;

export const ALL_LANGUAGES = "all";
